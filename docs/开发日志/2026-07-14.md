# 2026-07-14 开发日志

## Slice 2：pybind11 薄绑定补全与 Windows 验证

### 实现内容

- 为 `beamdrop_native.send(...)` 增加可选 `on_progress` Python 回调。
- 发送任务执行期间使用 `py::gil_scoped_release` 释放 GIL；C++ 进度回调调用 Python 函数前使用 `py::gil_scoped_acquire` 恢复 GIL。
- Python 回调抛出 `py::error_already_set` 时，将其转换为 binding 内部的 `PythonProgressCallbackError`；C++ `SendService` 将其归一为 `ServiceResult` 的 `ErrorCode::InternalError`，binding 最终抛出 `BeamDropError`。
- 新增 `ReceiverService` Python 持久对象，适配 `ReceiveServerService::start/status/stop`。Python 持有对象期间，接收服务持续监听；不暴露裸 socket、`TcpServer` 或 `Receiver`。
- 新增 `ReceiverState`、`ReceiverStatus`，并将接收服务的直接失败继续转换为带稳定错误码的 `BeamDropError`。
- 新增 `bindings/python/tests/test_beamdrop_native.py`，覆盖真实 loopback、进度回调、中文文件名、SHA256、回调异常和非法回调参数。
- 在 `bindings/python/CMakeLists.txt` 中新增 `beamdrop_python_binding_tests` CTest 条目，并通过 target 输出目录设置 `PYTHONPATH`。

### Windows 手工验证

- `ReceiverService.start/status/stop` 输出依次验证为 `RUNNING`、`RUNNING`、`STOPPING`，证明 Python 持有的服务对象不会像当前 CLI `serve` 一样在 `start()` 返回后立即析构。
- 同进程 loopback 成功传输 4 MiB 文件；发送进度收到：`TASK_STARTED`、多次 `TRANSFERRING`、`FILE_COMPLETED`、`TASK_COMPLETED`。
- loopback 接收文件 SHA256 与源文件一致。
- Python 回调故意抛出 `RuntimeError` 后，调用方收到 `BeamDropError(code=ErrorCode.INTERNAL_ERROR)`，Python 进程未崩溃。
- pytest 已通过，且真实使用中文文件名 `数据.bin` 验证 UTF-8 路径边界。
- 重新配置并执行 Windows CTest 后，`beamdrop_unit_tests`、`beamdrop_integration_tests`、`beamdrop_python_binding_tests` 共 3/3 通过；说明 CTest 能通过 `PYTHONPATH=$<TARGET_FILE_DIR:beamdrop_native>` 调用 pytest 并加载当前构建的 native 模块。

### 已知行为

- 回调异常处理保存 `py::error_already_set::what()` 后恢复并清理 CPython 错误状态，再转换为 `BeamDropError(ErrorCode.INTERNAL_ERROR)`；不再把预期的任务失败作为 unraisable traceback 输出到 stderr。
- `ReceiverService.stop()` 当前语义为请求 worker 停止，立即返回的状态可能是 `STOPPING`，不承诺同步等待至 `STOPPED`。
- CLI `serve` 现持有 `ReceiveServerService` 直到进程退出，并轮询服务状态；监听线程异常会使 CLI 以非零状态退出。正常退出方式为 Ctrl+C 或终止进程。

### 验证命令

```powershell
cmake --build build/windows-msvc2026-python --config Debug

$pyd = Get-ChildItem build\windows-msvc2026-python -Recurse -Filter 'beamdrop_native*.pyd' | Select-Object -First 1
$env:PYTHONPATH = $pyd.DirectoryName
& "$PWD\.venv\Scripts\python.exe" -m pytest bindings\python\tests -v
Remove-Item Env:PYTHONPATH
```

### 后续事项

- Python loopback 测试在比较接收文件 SHA256 前必须等待接收服务进入 `STOPPED`：发送端返回不等于异步接收 worker 已完成落盘和校验。
- Linux 验证已于 2026-07-14 完成：CMake 使用 `.venv-linux/bin/python` 构建模块，CTest 中 unit、integration、Python binding 三个测试均通过。`beamdrop_python_binding_tests` 成功 import Linux `.so` 并完成真实 loopback 验证。
- Linux 虚拟环境未安装 Python wheel 版 `pybind11`，因此 `python -m pybind11 --cmakedir` 输出 `No module named pybind11`；但系统安装的 `pybind11-dev 2.11.1` 已被 `find_package(pybind11 CONFIG REQUIRED)` 自动发现并成功构建。后续 Linux 构建命令不应强制传入空的 `pybind11_DIR`。
- Slice 2 所有 Windows / Linux 验收门禁已完成；下一步进入 Slice 3 跨宿主领域契约与前端端口。

## CLI `serve` 生命周期回归修复

- 修复 `run_serve()`：启动成功后不再立即销毁局部 `ReceiveServerService`，进程会持续持有监听服务。
- Windows MinGW 冒烟验证：子进程启动后仍存活，且 `127.0.0.1:19111` 处于 `Listen` 状态；随后强制终止子进程并清理临时目录。